# Я Бао Завари — статический frontend-прототип

GitHub Pages версия сайта чайной «Я Бао Завари» в Челябинске.

## Текущая структура

### Страницы
- `index.html` — главная;
- `shop/index.html` — прототип интернет-магазина и основной коммерческий каталог;
- `cart/index.html` — корзина интернет-магазина (noindex): состав, количество, удаление и промежуточный итог;
- `checkout/index.html` — оформление заказа без регистрации (noindex);
- `order-success/index.html` — demo-состояние успешной оплаты (noindex);
- `order-failed/index.html` — demo-состояние ошибки/отмены оплаты (noindex);
- `delivery/index.html` — доставка и самовывоз (noindex до подтверждения реальных условий);
- `menu.html` — статический legacy-снимок прежнего меню: `noindex,follow`, canonical на `/shop/`, production-редирект 301 остаётся этапом запуска;
- `about.html` — о чайной;
- `contacts.html` — контакты;
- `privacy.html`, `consent.html` — юридические страницы (noindex);
- `404.html` — страница ошибки (noindex).

### CSS
- `tokens.css` — дизайн-токены;
- `base.css` — reset, типографика, контейнеры и базовые utility;
- `components.css` — глобальные компоненты;
- `pages.css` — внутренние страницы;
- `responsive.css` — общий адаптив;
- `custom.css` — глобальные проектные уточнения;
- `home-v4.css` — только главная;
- `shop.css` — каталог `/shop/`, карточки товара, `/cart/`, `/checkout/`, demo payment result pages и `/delivery/`.

`sections.css` удалён в v4.36: после предыдущей чистки в нём оставался только FAQ главной, который перенесён в `home-v4.css`.

### JavaScript
- `app.js` — глобальная инициализация и подключение состояния корзины;
- `store.js` — единый store корзины: localStorage, add/remove/update quantity, totals и синхронизация badge;
- `cart.js` — `/cart/`: рендер состава заказа, сверка с `products.json`, количества, удаление, очистка и промежуточный итог;
- `checkout.js` — `/checkout/`: сверка корзины, форма покупателя, способ получения, сводка заказа и запуск demo-платёжного flow;
- `payment-demo.js` — sessionStorage-контракт demo-платежа: pending → success/failed без банковских данных и реального списания;
- `order-result.js` — `/order-success/` и `/order-failed/`: отображение результата demo-платежа; success очищает корзину, failed сохраняет её;
- `home.js` — главная и переиспользуемая инициализация `guides-v4` Swiper на товарных страницах; hero video/Fancybox/top-scroll активируются только при наличии соответствующей разметки;
- `menu.js` — legacy-меню: локальные фильтры статического снимка;
- `shop.js` — `/shop/` и demo-карточки товара: загрузка demo-товаров, поиск, тип/категория, сортировка, URL-state, галереи, выбор веса чая и количества штучных товаров;
- `components/forms/` — телефон, select, дата/время, validation и demo-form;
- `utils/` — общий initializer и единый scroll dispatcher.

## Зависимости
- Fancybox — локально в `assets/vendor/fancybox/`;
- Swiper — локально в `assets/vendor/swiper/`;
- дата и время — нативные `input[type="date"]` / `input[type="time"]`;
- Flatpickr не используется;
- каталог в GitHub Pages хранится статически; серверную синхронизацию с учётными данными планируется подключить на этапе WordPress;
- PT Sans и PT Serif подключены локально из `assets/fonts/`; внешней Google Fonts-зависимости в текущей версии нет.

## Важное перед production
1. Подключить реальную серверную отправку форм: сейчас формы работают как UI-прототип и данные не отправляют.
2. Проверить и заполнить юридические данные в `privacy.html` и `consent.html` перед production.
3. Перед публикацией актуальных цен, графика и событий сверять данные с официальными источниками проекта.
4. При переносе с GitHub Pages на основной домен обновить canonical, sitemap, robots и JSON-LD URL.

## v4.36 — финальный QA после рефакторинга
- исправлен modifier footer social styles: `--primary`;
- удалён последний точный CSS-дубль `top-scroll`;
- удалён неиспользуемый `sections.css`;
- удалены оставшиеся Flatpickr-стили после перехода на нативные date/time;
- удалены мёртвые lightbox/tag/price-line/feature-list стили;
- удалены 10 неиспользуемых legacy-изображений;
- выполнен полный статический QA проекта; браузерный smoke-тест нужно повторить после загрузки на GitHub Pages, потому что локальный Chromium в текущей среде блокирует localhost/file navigation.

## v4.37 production hardening

- доступность: skip-link на всех страницах;
- прямое подключение Google Fonts без CSS `@import`;
- intrinsic width/height для статических и динамических изображений;
- preload hero poster + preconnect для внешних ресурсов по месту использования;
- autocomplete для имени и телефона;
- Open Graph URL/site name и Twitter Card metadata;
- hero-видео перекодировано в H.264 1920×1080 с faststart для меньшего веса;
- визуальная структура и тексты не менялись.

## v4.38 technical SEO

- индексируемые страницы: главная, меню, о чайной, контакты;
- `booking`, `privacy`, `consent` и `404` сохраняют `noindex,follow`;
- sitemap содержит только индексируемые canonical URL;
- добавлены `CafeOrCoffeeShop`, `WebSite`, `WebPage` и `BreadcrumbList` в JSON-LD;
- бизнес-схема дополнена телефоном, изображением и ссылками на карты;
- Open Graph image metadata дополнена type/width/height/alt;
- улучшены SEO title/description у страниц «О чайной» и «Контакты»;
- canonical-схема и robots-политика не меняют структуру сайта.

## v4.39 interaction accessibility

- единый body-lock для мобильного меню и booking modal;
- мобильная навигация удерживает клавиатурный фокус внутри открытого overlay и закрывается при переходе на desktop;
- accordion автоматически получает `aria-controls`, `aria-labelledby` и region semantics;
- ошибки форм связаны с полями через `aria-describedby`;
- при невалидной отправке фокус переводится к первому проблемному полю;
- custom select получил полноценную клавиатурную навигацию и больше не создаёт скрытый дублирующий Tab-stop;
- добавлены заметные `:focus-visible` состояния без изменения обычного визуала.

## v4.40 runtime robustness

- исправлен первый фокус booking modal: скрытые hidden-поля больше не перехватывают попытку фокусировки;
- reset формы теперь очищает старые ошибки, `aria-invalid`, статус и loading-state;
- после успешной демо-отправки success-сообщение сохраняется до следующего взаимодействия/закрытия;
- глобальное сообщение об ошибке формы очищается, когда пользователь начинает исправлять поля;
- каталог различает свежий cache и просроченные данные;
- просроченные цены/остатки не используются как fallback;
- при offline каталог сразу показывает безопасный статический fallback, не ждёт сетевой timeout;
- после восстановления сети каталог автоматически пытается обновиться;
- параллельные повторные запросы каталога блокируются одним refresh promise.

## v4.40.1 W3C HTML validation patch

Исправлены 4 ошибки главной страницы из Nu Html Checker:

- два generic `div` с `aria-label` получили `role="group"`;
- карточка «Вдвоём или с друзьями» получила корректный интерактивный сценарий открытия booking modal;
- обработчик `[data-modal-open]` централизует открытие modal без отдельного перехода на страницу бронирования.

## v4.40.2 mobile booking UX patch

- полностью удалён sticky `.mobile-cta` со всех страниц;
- убран резерв `padding-bottom` под нижнюю sticky-кнопку;
- кнопка «Забронировать» добавлена внутрь `.mobile-nav` на всех 8 страницах;
- при нажатии кнопки мобильное меню сначала закрывается, затем открывается booking modal;
- удалены `initMobileCta`, CSS `.mobile-cta` и неиспользуемый `--z-mobile-cta`.

## v4.41 events

- добавлен блок «События и встречи» на главную;
- добавлена индексируемая страница `events.html`;
- добавлен общий Swiper-компонент для мероприятий;
- слайдер работает на главной и на странице мероприятий;
- в навигацию и футер добавлена ссылка «Мероприятия»;
- `events.html` добавлена в sitemap и получает WebPage/Breadcrumb JSON-LD;
- Event schema не добавлялась: подтверждённых дат и программ пока нет;
- карточки используют только уже заявленные на сайте форматы: музыкальные вечера, английский клуб, настольные игры, чайные клубы и камерные мастер-классы;
- неподтверждённые даты, цены, ведущие и программы не выдумывались.

## v4.41.1 events architecture patch

- `events.html` переведена со слайдера на последовательный список мероприятий;
- добавлен стилизованный пример пагинации для будущего большого архива;
- H1 страницы изменён на «Мероприятия»;
- breadcrumbs и hero `events.html` приведены к эталону `menu.html`;
- исправлен нижний отступ hero через те же `section section--dark section--compact inner-hero`;
- карточки слайдера на главной получили ссылки на `event.html`;
- создана эталонная страница одного мероприятия `event.html`;
- `event.html` пока `noindex,follow`, потому что реальных даты/времени/программы ещё нет;
- `Event` schema намеренно не используется до появления подтверждённых данных;
- Swiper полностью удалён из `events.html`, но сохранён в блоке мероприятий на главной.

## v4.41.2 events UX patch

- в `event.html` добавлена кнопка «Записаться» с event-mode booking modal;
- исправлен `aria-current`: на `events.html` и `event.html` активен только пункт «Мероприятия»;
- действие карточки списка перенесено внутрь `.event-list-card__content` и теперь подписано «Перейти →»;
- действие остаётся видимым на планшетах и мобильных;
- пример пагинации использует реальные ссылки, без подчёркивания, с hover/focus состояниями;
- удалён поясняющий текст под пагинацией;
- мобильная пагинация уплотнена;
- при `max-width:1024px` убраны лишние нижние margin у H1 и текста inner hero, потому что расстояние уже задаёт grid `gap`;
- `.eyebrow` удалены из всех `.inner-hero`.

## v4.41.3 event aside button contrast patch

- вторичная кнопка «К мероприятиям» в `event.html` переведена с `button--light` на контурную `button--outline-walnut`;
- на светлом фоне кнопка теперь имеет заметную ореховую рамку и текст;
- hover заполняет кнопку ореховым цветом с белым текстом.

## v4.41.4 event modal locked fields

- поля booking modal, которые нельзя редактировать (`readonly`/`disabled`), теперь имеют фон `#e2e2e2`;
- добавлены более спокойные border/focus состояния, чтобы визуально отличать заблокированные поля от доступных для ввода.

## v4.41.5 modal scrollbar compensation

- при открытии modal/mobile-nav ширина исчезающего классического scrollbar вычисляется до `overflow:hidden`;
- это значение передаётся в `--scrollbar-compensation`;
- `body` получает такой же `padding-right`, поэтому основная страница не расширяется и не прыгает вправо;
- fixed header и `.top-scroll` получают такую же компенсацию, чтобы оставаться выровненными с контентом;
- при закрытии последнего overlay компенсация полностью снимается;
- на устройствах с overlay-scrollbar ширина компенсации равна `0px`.

## v4.41.6 homepage events/gallery UX

- у карточек мероприятий добавлен запас сверху, поэтому hover `translateY` больше не обрезается;
- `events-slider__controls` центрированы на всех ширинах;
- `events-slider-shell` на ширинах до 1480px получает такой же расширенный gutter-подход, как `guide-grid-v4`;
- мобильная кнопка «Открыть афишу» растягивается на всю ширину;
- `reveal` удалён только с `.guide-card-v4`;
- заголовок блока изменён на «События и встречи»;
- на первом фото галереи при ширине до 768px появляется подсказка «Нажмите — открыть галерею»;
- на ширине до 560px галерея сохраняет двухколоночную композицию: 100px + 100px, gap 10px, radius 12px, большая фотография остаётся на две строки.

## v4.41.7 homepage slider/gallery/mobile map patch

- у `.event-card--link:hover` полностью убран `transform: translateY(-4px)`, поэтому карточка больше не может обрезаться сверху;
- на ширине до 1480px расширение по gutter перенесено с `.events-slider-shell` на `.events-slider.swiper` по той же логике, что у `.guide-grid-v4.swiper`;
- `.events-slider-shell` больше не режет края и остаётся `overflow: visible`;
- подсказка на первом фото сокращена до «Открыть галерею»;
- на ширине до 560px `.local-v4__map iframe` и `.local-v4__visual` получают `min-height: 300px` и `border-radius: 12px`.

## v4.42 maps/blog/navigation

- карта на главной и `contacts.html` заменена с Яндекс Карт на виджет 2ГИС для карточки Я Бао Завари;
- «Мероприятия» во всех desktop/mobile шапках теперь ведут только на `events.html`;
- «Блог» во всех desktop/mobile шапках теперь ведёт на `blog.html`;
- в footer-группу «Посетить» на всех страницах добавлена ссылка «Главная» первой;
- 6 карточек `.guide-card-v4` на главной стали ссылками на `article.html`;
- в `.guides-v4` добавлена кнопка «Перейти в блог»;
- создана страница `blog.html` со списком материалов;
- создана эталонная `article.html`; она пока `noindex,follow` до финальной редакционной подготовки;
- `blog.html` добавлена в sitemap, `article.html` намеренно не добавлена.

## v4.42.1 blog/pagination/navigation patch

- кнопка «Перейти в блог» на главной стилизована как действие `.event-list-card__arrow` и получила стрелку;
- «Церемония» удалена из desktop/mobile шапки и из footer-links на всех страницах;
- `.events-pagination` переименована в универсальную `.pagination`;
- универсальная пагинация добавлена на `blog.html`;
- карточки блога переведены на ту же структуру и визуальную систему, что `.event-list-card`.

## v4.42.2 homepage/blog/article/menu patch

- «Перейти в блог» на главной переведена на стандартную кнопку главной по схеме section-tail;
- восстановлена пагинация слайдера мероприятий: исправлен `data-pagination` на ожидаемый `data-events-pagination`;
- в `article.html` добавлен Swiper «Похожие статьи»;
- brand на внутренних страницах синхронизирован с эталонной главной на диапазоне 768–1366px;
- `.page-menu .catalog-notice` вертикально центрирует индикатор и текст, включая мобильную версию.

## v4.42.3 homepage blog CTA / brand patch

- «Перейти в блог» использует стандартный `section-tail`, выравнивается вправо и имеет `1px solid var(--color-line)`;
- удалено старое центрирование `.guides-v4__tail`;
- `.brand small` больше не скрывается при `max-width:390px`;
- удалён homepage-specific `display:block` для `.brand small`, который создавал лишнюю специфичность;
- subtitle бренда теперь управляется одной общей базовой логикой на всех страницах.

## v4.42.4 doctype validation patch

- удалены накопившиеся повторные `<!DOCTYPE html>` во всех HTML-файлах;
- каждый HTML-файл теперь содержит ровно один doctype в первой строке;
- причина была в промежуточных скриптах сборки: BeautifulSoup сохранял существующий doctype, а сериализация дополнительно дописывала новый.

## v4.42.5 local fonts

- Google Fonts (`fonts.googleapis.com` / `fonts.gstatic.com`) полностью удалён из HTML;
- PT Sans и PT Serif подключаются локально через `@font-face` в `assets/css/tokens.css`;
- для каждой гарнитуры подключены Regular, Italic, Bold и Bold Italic;
- источники: EOT + WOFF2 + TTF;
- в `assets/fonts/README.txt` указан полный список ожидаемых имён файлов;
- сами font-файлы в этот архив не включены: их нужно добавить в `assets/fonts/` перед публикацией.

## v4.43 static menu snapshot

- клиентское чтение Google Sheets через GViz полностью отключено;
- из `menu.html` удалён preconnect к `docs.google.com`;
- текущие 71 позиции каталога зафиксированы в HTML как статический снимок;
- позиции, связанные с кальяном/табаком/никотином, в публичный каталог не добавлены;
- бейджи наличия удалены, чтобы статическая версия не показывала устаревающие остатки;
- фильтры категорий продолжают работать локально через `menu.js`;
- исправлен уже существующий фильтр «Бабл ти»: соответствующие текущие позиции теперь относятся к нему, а не к «Лимонадам»;
- удалены неиспользуемые `config.catalog.js`, `components/catalog.js` и `components/catalog/`;
- серверную синхронизацию каталога и наличия следует подключать уже при интеграции на WordPress.

## v4.44 — этап 50: product data contract

- добавлена папка `data/` без изменений существующей визуальной вёрстки;
- `data/products.schema.json` фиксирует JSON Schema Draft 2020-12 для будущего магазина;
- `data/products.json` содержит 10 явно помеченных demo-сущностей: обычный и прессованный чай, посуда и аксессуары;
- для чая зафиксированы `sale_mode=weight`, минимум 50 г и варианты с постоянным `variant_id`;
- для посуды/аксессуаров зафиксированы `sale_mode=unit` и штучный `quantity`;
- поддержаны статусы `active`, `out_of_stock`, `inactive`, `missing_from_feed`, `archived`;
- реальные товары, реальные изображения, `/shop/`, корзина и checkout на этом этапе не добавлялись.

## v4.45 — этап 51: глобальная навигация магазина

- в desktop header, mobile navigation и footer пункт `Меню` заменён на `Магазин` со ссылкой на `/shop/`;
- в шапку добавлен статический индикатор корзины с количеством `0` и data-hook `data-cart-count` под будущий store state;
- корзина на этом этапе не реализована: индикатор не кликабельный, `localStorage`, add/remove/update и `/cart/` не подключались;
- `sitemap.xml` и HTML-карта сайта теперь указывают `/shop/` вместо `menu.html`;
- добавлен переходный `shop/index.html`, чтобы новая глобальная ссылка не вела на 404; реальный каркас каталога будет собран только на этапе 52;
- контекстные ссылки внутри статей и CTA на старое `menu.html` на этом этапе намеренно не переносились; миграция выполнена позже на этапе 62.


## v4.46 — этап 52: каркас каталога /shop/

- переходный экран `/shop/` заменён на полноценный каркас каталога без перехода к карточке товара, корзине и checkout;
- добавлены hero/intro, фильтры `Все / Чай / Посуда / Аксессуары` и адаптивная сетка карточек;
- каталог загружается из `data/products.json`, при этом публично показываются только статусы `active` и `out_of_stock`; `inactive`, `missing_from_feed` и `archived` скрыты;
- тестовые товары явно помечены как demo, реальные фото и реальный ассортимент не подставляются;
- реализованы состояния loading, empty, error и out-of-stock;
- фильтры работают кнопками с `aria-pressed`; Arrow Left/Right, Home и End перемещают фокус между фильтрами;
- поиск, сортировка, URL/query-state, карточка товара, выбор веса и add-to-cart намеренно не реализованы — это следующие этапы.

### v4.46.1 — QA-правка этапа 52: загрузка каталога

- после мобильного QA убран `cache: 'no-store'`, из-за которого Safari каждый раз принудительно ждал сетевую загрузку `products.json`;
- для `products.json` используется стандартная политика браузерного кэша (`cache: 'default'`);
- в `/shop/` добавлен `preload` для `products.json`, чтобы загрузка данных начиналась ещё до выполнения `shop.js`;
- добавлен таймаут 8 секунд через `AbortController`: skeleton не может висеть бесконечно, при проблеме показывается уже существующий error-state с кнопкой повторной загрузки;
- структура этапа 52 не расширялась: поиск, сортировка, query-state, карточка товара и корзина по-прежнему не реализованы.

### v4.46.2 — QA-правка этапа 52: hidden-state в Safari

- по повторной записи с iPhone уточнена причина: данные каталога уже были загружены (счётчик показывал `7 товаров`), но skeleton продолжал отображаться поверх/перед карточками;
- причина — авторские `display:grid` / `display:flex` для элементов каталога могли переопределять стандартное браузерное поведение атрибута `hidden`, особенно заметно в Safari;
- для `/shop/` добавлено явное правило `.page-shop [hidden]{display:none!important}`, поэтому loading/grid/filter/error/empty состояния теперь переключаются однозначно;
- оптимизация v4.46.1 (`cache: default`, preload и timeout) сохранена, но не считается основной причиной визуального бага;
- функциональность этапа 53 не добавлялась.

## v4.47 — этап 53: поиск, сортировка и фильтры каталога

- в `/shop/` добавлен поиск по названию товара;
- фильтр по типу (`Все / Чай / Посуда / Аксессуары`) работает совместно с динамическим фильтром по категории;
- категории строятся только из публичных demo-товаров текущего выбранного типа;
- добавлена сортировка: исходный порядок, название А–Я, цена по возрастанию и цена по убыванию;
- поиск, тип, категория и сортировка комбинируются между собой;
- состояние каталога сохраняется в query string через `q`, `type`, `category`, `sort` и восстанавливается при открытии URL;
- параметры со значениями по умолчанию не засоряют URL, некорректные значения отбрасываются, сторонние query-параметры сохраняются;
- добавлены общий reset и empty-state для нулевой выдачи;
- сохранены ограничения публичного каталога: видимы только `active` и `out_of_stock`;
- сохранены Safari hidden-state fix v4.46.2, preload, `cache: default` и 8-секундный timeout загрузки;
- во время полного QA исправлен оставшийся в `blog-kak-vybrat-kitayskiy-chay.html` дублирующий `<!DOCTYPE html>` из старого baseline;
- пагинация, `/shop/{slug}/`, выбор веса, store state, корзина и checkout не добавлялись.

## v4.47.1 — UX/consistency patch after Stage 53

- удалён отдельный demo-notice над каталогом;
- устранена двойная стрелка в shop custom select;
- мобильные controls магазина уплотнены: поиск на всю строку, категория и сортировка рядом;
- карта на главной переведена с iframe 2ГИС на Яндекс Карты;
- footer legal перенесён в footer-brand на всех HTML-страницах;
- во все footer добавлена ссылка «Разработано Abzalov-Lab»;
- cart indicator переставлен перед кнопкой «Забронировать» в header;
- карточка «Просто зайти на чай» на главной теперь ведёт в `/shop/`;
- карточки магазина используют существующие изображения из старого `/menu/`;
- custom select теперь пересобирает UI-options при динамическом изменении native select, поэтому категории Stage 53 синхронизируются корректно.

## v4.47.2 — mobile catalog filters UX patch

- на мобильных поиск остаётся всегда видимым, а категория и сортировка перенесены в раскрываемый блок «Фильтры»;
- при активной категории/сортировке кнопка показывает количество активных расширенных фильтров;
- поиск и custom-select приведены к единой высоте на всех breakpoint;
- удалены конфликтующие mobile CSS-правила, из-за которых input и select получали разные высоты;
- mobile type-filter chips уплотнены: меньшая высота, padding и gap, без лишнего горизонтального overflow;
- Stage 54 не затронут.
## v4.47.3 — mobile typography, legal data and booking cleanup

- мобильный текст поиска и custom-select в `/shop/` уменьшен; иконка поиска и внутренние отступы стали компактнее;
- реквизиты оператора персональных данных в `privacy.html` и `consent.html` оформлены семантической таблицей;
- для всех `h1/h2/h3` внутри `main` введена единая мобильная шкала заголовков; footer, burger и dialogs не затронуты;
- мобильный `.lead` приведён к единому более компактному размеру;
- `booking.html` удалён; карточка «Вдвоём или с друзьями» на главной открывает booking modal через локальный `#booking-modal` без перехода на отдельную страницу;
- ссылка на `booking.html` удалена из человекочитаемой карты сайта и технических inventory-файлов;
- этап 54 не начинался.

## v4.47.4 — типографика и компактность mobile QA

- карточки мероприятий и карточки списка блога используют `h3` вместо `h2`; отдельный размер заголовка у `.event-list-card` удалён, используется общий `h3`;
- заголовки `.sitemap-card` переведены на `h3`, отдельный размер также удалён;
- из единой мобильной шкалы заголовков убраны `!important`, размеры H1/H2 уменьшены, а основной `main` получил более плотный `line-height`;
- `.contact-card` сделана компактнее за счёт существующих padding/gap/margin и `--radius-sm`; новых компонентных типографических размеров не добавлено.

## v4.47.5 — mobile card/lead cleanup

- удалена отдельная типографическая стилизация `.article-page__content .lead`; статьи теперь используют общий `.lead` и единую mobile-типографику без локального переопределения;
- общий `.card__body` на мобильных приведён к компактному `padding:18px` внутри существующего глобального responsive breakpoint;
- новых компонентных размеров и отдельных mobile-типографических правил не добавлялось; этап 54 не начинался.

## v4.48 — этап 54: базовый шаблон карточки товара

- для всех 7 публичных demo-товаров созданы URL `/shop/{slug}/`;
- карточки каталога теперь являются ссылками на соответствующие товарные страницы;
- базовая карточка содержит галерею из доступных изображений, название, цену, наличие, описание, характеристики, CTA, похожие товары и связанные статьи;
- для чая варианты веса на этом этапе показываются только как справочные данные — интерактивного селектора ещё нет;
- add-to-cart, store state и корзина не добавлялись;
- demo-товарные страницы имеют `noindex,follow`; `Product` schema не добавлялась до появления реальных товарных данных;
- `inactive`, `missing_from_feed` и `archived` не получают публичных товарных страниц;
- отдельные размеры `h1/h2/h3` для карточки товара не создавались: используется общая типографическая шкала проекта.

## v4.48.1 — QA компоновки карточки товара

- desktop hero товарной страницы уплотнён без изменения глобальных размеров заголовков;
- основной product grid на desktop получил больше ширины под изображение, меньший gap и вертикальное выравнивание контента;
- первый экран карточки товара стал компактнее за счёт уменьшенного вертикального padding product-detail;
- на мобильных секции товарной страницы, section-heading и расстояния между карточками уплотнены;
- глобальная типографика, footer, burger, выбор веса и cart logic не менялись; этап 55 не начинался.


### v4.48.2 — QA/UX-правка этапа 54: единый hero и повторное использование guides-v4

- `/shop/` переведён на общий `inner-hero`, поэтому хлебные крошки магазина и карточек товара занимают одно и то же место на всех разрешениях;
- из `shop.css` удалены локальные переопределения `inner-hero` товарных страниц из v4.48.1 — hero снова использует эталонную общую вёрстку внутренних страниц;
- описание товара перенесено под `.product-detail__grid` внутрь той же секции `.product-detail`, отдельная секция описания удалена;
- блок «Материалы по теме» на всех 7 demo-карточках переведён на уже существующее решение `.guides-v4` + Swiper с теми же классами, CSS и JS, что используются на главной;
- product-specific стили `.product-related-link` и привязка мобильного gap к `.page-product .cards-grid` удалены;
- базовый `.cards-grid--three` в `pages.css` не удалён: аудит показал, что он всё ещё используется в `chaynaya-ceremoniya.html` и `contacts.html`, поэтому его удаление сломало бы существующие страницы.

### v4.48.3 — QA/UX-правка этапа 54: идентичный guides-v4 в товаре

- блок «Полезно» на всех 7 товарных страницах заменён на точную HTML-копию существующего `guides-v4` с главной страницы;
- сохранены те же `guide-grid-v4`, `guide-card-v4`, метаданные карточек, пагинация, стрелки, `section-tail` и ссылка «Перейти в блог»;
- изменены только относительные `href` для уровня `/shop/{slug}/`;
- новые CSS и JS не создавались, существующие `home-v4.css`, Swiper и `home.js` переиспользуются без изменений.

### v4.48.4 — CSS cleanup перед этапом 55

- выполнен аудит всех 8 CSS-файлов относительно baseline `main` commit `6ad2b36c6b37682a19c6c64c9a64ea13af0db696`;
- удалены неиспользуемые стили удалённой `booking.html`, старого `.shop-catalog__notice`, `.section--olive` и старого `.shop-hero`;
- mobile header приведён к одному источнику responsive-правил: убраны повторные `menu-toggle`/brand overrides из `custom.css`, при этом текущие размеры на breakpoint сохранены;
- удалены мёртвые и повторные mobile-правила footer и shop controls, а `hidden` в магазине оставлен единым через `.page-shop [hidden]`;
- из product page удалены общие mobile overrides секций/section-heading, из-за которых переиспользованный `guides-v4` отличался по вертикальному ритму от главной;
- удалены повторные декларации `home-v4.css` (`tea-card`, `beginner`, `space-gallery`);
- `.article-page__content h3` больше не задаёт отдельный `font-size` и использует общую шкалу заголовков;
- после cleanup проектных CSS-классов, не встречающихся в HTML/JS, не осталось; исключения — runtime-классы Swiper;
- новые компоненты, новые CSS-файлы и новые JS не создавались; этап 55 не начинался.
### v4.48.5 — QA/UX-правка этапа 54: галереи товара и карточек магазина

- на всех 7 публичных demo-товарах добавлена галерея в стиле товарной страницы: крупное изображение и вертикальные миниатюры слева на desktop/tablet; на мобильных миниатюры переходят под основное изображение;
- галерея товара работает через уже существующий `shop.js`: выбор миниатюры, клавиатурная навигация и свайп на touch-устройствах; отдельный JS-файл не создавался;
- карточки `/shop/` получили marketplace-preview по образцу MO Car Rent: горизонтальная позиция курсора выбирает изображение, снизу показывается прогресс, на touch работает свайп;
- demo-товарам добавлены дополнительные ссылки на уже существующие изображения из `assets/images/`; новые image-файлы не создавались;
- Open Graph, цены, статусы, варианты, cart/store state и этап 55 не менялись.

### v4.49 — этап 55: варианты веса чая от 50 г

- на публичных товарных страницах чая добавлен выбор только доступных весовых вариантов из `products.json`;
- минимальный допустимый вес в UI — 50 г и одновременно не меньше `min_weight_g` конкретного товара;
- при выборе 50/100/250 г цена обновляется из цены конкретного `variant_id`, без арифметического пересчёта базовой цены;
- выбранный `variant_id` сохраняется в скрытом поле и `data-selected-variant-id` как контракт для этапа 57;
- selector использует существующие `shop-filter`/`shop-filter-bar`, новые отдельные UI-компоненты и JS-файлы не создавались;
- для `out_of_stock` чая без вариантов показывается состояние «Сейчас нет доступных вариантов веса»; фиктивные варианты не создаются;
- корзина, add-to-cart, localStorage, количество штучных товаров и checkout не добавлялись — это следующие этапы;
- `products.json` не менялся: этап использует существующий data contract.

### v4.50 — этап 56: посуда и аксессуары

- для публичных `ware` и `accessory` с `sale_mode=unit` добавлен отдельный выбор количества в штуках;
- количество ограничено фактическим `quantity` конкретной demo-сущности: нельзя выбрать меньше 1 или больше доступного остатка;
- кнопки −/+ синхронизируются с числовым полем, крайние действия блокируются на минимуме и максимуме;
- выбранное количество сохраняется в `name=quantity` и `data-selected-quantity` как контракт для будущего store state этапа 57;
- при изменении количества отправляется `shop:quantitychange` с `productId`, `quantity` и `maxQuantity`;
- для `out_of_stock` штучного товара интерактивный выбор количества не создаётся;
- весовой UI чая и штучный UI разделены: у `ware/accessory` нет граммов и `variant_id`, у чая не появляется штучный stepper;
- дополнительные unit-варианты не выдумывались: в текущем `products.json` ни у одной публичной посуды/аксессуара нет `variants`, поэтому отдельный selector не выводится;
- `products.json`, изображения, корзина, localStorage и checkout не менялись — store state остаётся этапом 57.


### v4.51 — этап 57: store state и localStorage

- добавлен единый `assets/js/store.js` с версионированным ключом `yabao:cart:v1`;
- store поддерживает add/remove/update quantity/clear, сумму заказа и общее количество товаров;
- для весового товара `variant_id` обязателен и входит в ключ строки корзины: разные веса одного чая сохраняются как разные варианты;
- для unit-товаров в корзину передаётся выбранное количество, а повторное добавление не может превысить текущий `quantity`;
- доступные товарные страницы получили кнопку «Добавить в корзину», `out_of_stock` остаются недоступны;
- `cart-indicator` на всех страницах синхронизируется с localStorage при загрузке и между вкладками;
- отдельная `/cart/` и checkout не создавались — это следующие этапы;
- `products.json`, изображения и CSS не менялись.


### v4.51.1 — UX-правка этапа 57: остаток уже в корзине

- для unit-товаров кнопка добавления учитывает количество этого товара, уже лежащее в localStorage-корзине;
- если выбранное количество больше оставшегося доступного остатка, интерфейс заранее сообщает «Можно добавить ещё N шт.», а кнопка предлагает добавить только доступное количество;
- после достижения полного остатка кнопка блокируется и показывает «Максимум в корзине», вместо ложного «Добавлено» без изменения счётчика;
- состояние автоматически обновляется по `shop:cartchange`, поэтому после будущего уменьшения/удаления товара из корзины кнопка снова станет доступной;
- числовые остатки для весовых вариантов не выдумывались: у текущих tea variants в data contract есть только `stock_status`, поэтому для них остаётся «В наличии / Нет в наличии» до подключения реальных stock-данных;
- `/cart/` и checkout не добавлялись; этап 58 не начинался.

### v4.52 — этап 58: страница /cart/

- создана `/cart/` с `noindex,follow`: состав заказа, варианты, количество, цена за единицу, сумма строки и промежуточный итог;
- индикатор корзины в header на всех страницах стал ссылкой на `/cart/`, сохранив текущий badge из store state;
- количество можно менять прямо в корзине, unit-товары учитывают текущий числовой остаток, позиции можно удалять, всю корзину — очищать с подтверждением;
- `/cart/` повторно читает текущий `products.json` и поддерживает состояния этапа 58: «цена изменилась», «вариант недоступен», «товар закончился»;
- если unit-остаток стал меньше количества в корзине, строка сообщает актуальный доступный остаток и временно не включается в промежуточный итог, пока количество не исправлено;
- при изменении цены в промежуточный итог входит актуальная цена из `products.json`, а старая сохранённая цена остаётся видна в предупреждении;
- недоступные товары/варианты не удаляются автоматически: пользователь видит проблему и может удалить позицию вручную;
- если актуальные данные каталога временно не загрузились, корзина не пропадает и показывает сохранённые snapshot-данные с отдельным предупреждением;
- store получил только совместимое расширение `updateCartItemQuantity(..., { maxQuantity })` для актуализации unit-лимита при редактировании корзины;
- checkout, доставка и платёжный flow не добавлялись — это следующие этапы;
- `products.json`, schema, изображения и sitemap не менялись; `/cart/` намеренно не добавлен в sitemap как noindex-страница.



## v4.52.1 — Stage 58 UX fix

- `/cart/`: compact responsive summary, tablet stack breakpoint, rebuilt mobile cart cards.
- Weight items now show the total grams already in cart (`250 г × 3 = 750 г`) and quantity is labeled as packages.
- Added a shared mini-cart drawer from the header cart indicator on non-cart pages. It uses the same Stage 57 store state, supports +/- and remove, and links to `/cart/`.
- Checkout and one-click order flow are intentionally not implemented in this pass.

## v4.52.2 — Stage 58 mobile interaction fix

- `/cart/`: cart item cards now use the shared small radius token instead of the larger card radius on desktop and mobile.
- Quantity stepper buttons use `touch-action: manipulation` to suppress double-tap zoom on touch devices while keeping normal page pinch zoom available.
- Added user-select protection on +/- buttons to avoid accidental text selection during rapid taps.
- Cart/store logic, product data, checkout and one-click order flow are unchanged.

### v4.53 — этап 59: `/checkout/`

- Добавлена noindex-страница `/checkout/` с оформлением без обязательной регистрации.
- Поля: имя, телефон, email, способ получения, адрес при доставке, комментарий и согласие на обработку данных.
- Самовывоз/доставка на этом этапе — только интерфейсный выбор; реальные зоны, стоимость и сроки доставки не выдумываются и остаются этапом 60.
- Состав заказа и итог читаются из общего cart store; весовые позиции показывают вес × количество.
- Текущие цены/наличие повторно сверяются с `products.json`; недоступные позиции блокируют CTA и отправляют пользователя обратно в корзину.
- `/cart/` и mini-cart получили переход «Оформить заказ» на `/checkout/`.
- Отправка заказа, CRM/email, аккаунт покупателя, банковские поля и платёжный flow не подключались.


### v4.54 — этап 60: доставка / самовывоз

- создана отдельная `/delivery/` с `noindex,follow`: самовывоз, доставка и честно обозначенные неподтверждённые условия;
- самовывоз использует уже известный адрес чайной: Челябинск, Кирова, 94;
- реальные зоны, стоимость, сроки, интервалы и пороги бесплатной доставки не выдумывались и не участвуют в расчёте заказа;
- checkout получил более явные карточки способов получения, отдельное состояние выбранного способа и ссылку на `/delivery/`;
- при выборе самовывоза показывается адрес чайной и ссылки на карты, при выборе доставки появляется обязательное поле адреса и пояснение о неподключённом тарифе;
- сводка checkout показывает выбранный способ получения; итог по-прежнему содержит только товары;
- ссылка «Доставка и самовывоз» добавлена в общий footer всех страниц; `/delivery/` не добавлен в XML sitemap до подтверждения реальных коммерческих условий;
- cart/store, ассортимент, цены, изображения, аккаунты, оплата и one-click flow не менялись.


### v4.54.1 — mobile spacing fix этапа 60

- На `/delivery/` уменьшен чрезмерный вертикальный разрыв между блоком «Как это работает» и следующим разделом «Условия доставки» на экранах до 640 px.
- Правка локальна для страницы доставки: нижний padding `delivery-process` — 20 px, верхний padding следующей `section` — 28 px.
- Desktop/tablet, checkout, cart/store, данные товаров и изображения не менялись.

### v4.55 — этап 61: demo-платёжный flow

- кнопка checkout изменена на «Перейти к оплате»: она по-прежнему не создаёт реальный платёж и не списывает деньги;
- после успешной валидации checkout открывается локальный demo-блок платёжного провайдера с двумя явными исходами: success и failed/cancelled;
- добавлен `assets/js/payment-demo.js` с sessionStorage-контрактом `yabao:payment-demo:v1`: reference, status, amount, currency, itemCount, fulfillment и timestamps; персональные данные покупателя в demo payment state не сохраняются;
- добавлены noindex-страницы `/order-success/` и `/order-failed/`, соответствующие целевой URL-архитектуре;
- успешный demo-исход очищает корзину только после перехода на `/order-success/`; неуспешный исход оставляет корзину без изменений и позволяет повторить checkout;
- прямое открытие result URL без активного demo-контекста безопасно: корзина не меняется, показывается fallback-состояние;
- банковские поля, платёжный iframe/API, backend order creation, email/CRM и реальное списание денег не добавлялись; это остаётся production-этапом подключения платёжного провайдера;
- `/order-success/` и `/order-failed/` намеренно не добавлены в sitemap как noindex-служебные страницы.

### v4.56 — этап 62: SEO магазина и перелинковка

- `/shop/` закреплён как единый коммерческий хаб под кластеры «китайский чай в Челябинске / магазин китайского чая / купить китайский чай», без создания отдельных индексируемых category URL;
- title, description, Open Graph/Twitter, WebPage JSON-LD, H1 и текстовые блоки `/shop/` разведены с локальным интентом главной: главная остаётся про чайную и визит, магазин — про выбор товаров;
- в `/shop/` добавлены быстрые ссылки на существующие query-state фильтры «Китайский чай / Пуэр / Чайная посуда / Аксессуары» и блок гидов по выбору/завариванию;
- все 69 внутренних `href` на legacy `menu.html` перенесены на `/shop/` или существующие фильтры `/shop/`; старый `/menu/` больше не получает внутренний ссылочный вес;
- `menu.html` переведён в `noindex,follow` и получил canonical на `/shop/` до будущего production 301;
- ключевые информационные статьи получили контекстные ссылки в магазин, включая «Как выбрать китайский чай», «Что такое пуэр», «Как заваривать пуэр», материалы по Шу/Шэн, Габа, улунам и белому чаю;
- для категорий, которых нет в текущем demo `products.json` (например Габа/улун), не создаются ложные точные фильтры: ссылки ведут в общий чайный каталог, а текст не обещает отсутствующие позиции;
- `about.html` и посадочная чайной церемонии получили/сохранили контекстный переход в магазин;
- Product schema не добавлялась: все demo product pages остаются `noindex,follow`; `/cart/`, `/checkout/`, `/order-success/`, `/order-failed/` также остаются noindex;
- `products.json`, schema, cart/store, checkout/payment flow и изображения не менялись.

