# Hero-блок «Я Бао Завари»

Адаптивный прототип hero-блока с шапкой, фоновым видео, мобильным меню и демонстрационной формой бронирования.

## Запуск

### Самый простой способ

Откройте `index.html` в браузере. Для корректной работы JavaScript-модулей рекомендуется запуск через локальный сервер.

### Через Python

```bash
python -m http.server 8080
```

Затем откройте `http://localhost:8080`.

### Через VS Code

Установите расширение Live Server и запустите `index.html` через **Open with Live Server**.

## Загрузка на GitHub Pages

1. Создайте новый публичный репозиторий.
2. Загрузите в корень репозитория все файлы и папки из архива.
3. Откройте **Settings → Pages**.
4. В разделе **Build and deployment** выберите **Deploy from a branch**.
5. Выберите ветку `main` и папку `/root`.
6. Сохраните настройки.

## Структура

```text
assets/
  icons/
  images/
css/
  components/
  sections/
  base.css
  responsive.css
js/
  components/
  main.js
index.html
README.md
```

## Что уже реализовано

- семантический hero-блок и один `h1`;
- адаптивная шапка и мобильное меню;
- фоновое видео с паром и свечой;
- неподвижная композиция без курсорного параллакса;
- поддержка `prefers-reduced-motion`;
- модальное окно бронирования;
- маска телефона и клиентская валидация;
- видимые focus-состояния и управление с клавиатуры;
- WebP-постер первого кадра;
- базовые метаданные для SEO.

## Перед публикацией реального сайта

- заменить демонстрационное изображение на утверждённое фото без встроенных интерфейсных элементов;
- заменить временный знак на официальный логотип;
- подключить отправку формы к WordPress/CRM/почте;
- добавить реальную страницу политики обработки персональных данных;
- проверить актуальные телефон, адрес, график и формулировки;
- подготовить отдельное Open Graph изображение;
- наполнить остальные разделы сайта.

## Видео в hero-блоке

Фоновое видео находится в `assets/video/`:

- `hero-tea-loop.mp4` — исходное видео без повторного сжатия;
- `assets/images/hero-video-poster.webp` — первый кадр высокого качества, который показывается до запуска видео.

MP4 заменён на улучшенную версию `Yabao-video-v1.mp4` и сохранён без повторного сжатия. Параметры исходника: 910×512 px, 30 FPS, около 3,59 Мбит/с, длительность 5,04 секунды. Качество соответствует загруженному файлу.

Видео подключено с атрибутами `autoplay`, `muted`, `loop` и `playsinline`. Камера и контейнер hero не двигаются: ранее использовавшийся курсорный параллакс отключён. На мобильных устройствах до 767 px и при `prefers-reduced-motion: reduce` используется статичное изображение, чтобы не расходовать трафик и батарею. Видеопроигрывание автоматически останавливается, когда hero уходит за пределы экрана или вкладка становится неактивной.
